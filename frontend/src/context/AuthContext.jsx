// Only the context object — nothing else.
// No component, no hook. This file satisfies react-refresh completely.

import { createContext } from 'react'

export const AuthContext = createContext(null)